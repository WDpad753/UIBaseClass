<Window x:Class="UIBaseClass.MessageBox.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIBaseClass.MessageBox"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Height="150" Width="300"
        AllowsTransparency="True" Background="Transparent" WindowStyle="None" WindowStartupLocation="CenterScreen"
        MouseMove="Window_MouseMove" MouseUp="Window_MouseUp" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Custom Message Box Dictionaries -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.CheckBox.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border x:Name="AddPopuproundBorder" BorderThickness="0" Background="White">
        <Border.OpacityMask>
            <VisualBrush>
                <VisualBrush.Visual>
                    <Border CornerRadius="5" Height="50" Width="100" Background="White"/>
                </VisualBrush.Visual>
            </VisualBrush>
        </Border.OpacityMask>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="70*"/>
                <RowDefinition Height="40*"/>
            </Grid.RowDefinitions>

            <!-- Top MessageBox Header -->
            <Grid Grid.Row="0" Background="LightGray" Opacity="0.8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10*"/>
                    <ColumnDefinition Width="50*"/>
                    <ColumnDefinition Width="10*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Grid.Column="0" Kind="Application" Foreground="Black" Width="50" Height="25" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Transparent"/>
                <TextBlock Name="txtTitle"  Grid.Column="1" Text="Message Title" Foreground="Black" VerticalAlignment="Center" FontWeight="Bold" FontSize="15"/>
                <!--<materialDesign:PackIcon Grid.Column="2" Kind="CloseCircle" Foreground="Black" Background="Transparent" VerticalAlignment="Center" HorizontalAlignment="Center" Height="25" Width="25" MouseLeftButtonUp="MouseLeftButtonUp"/>-->
                <Button Grid.Column="2" Background="Transparent" BorderBrush="Transparent" Click="CloseButton_Click" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0" Width="25" Height="25">
                    <materialDesign:PackIcon Kind="CloseCircle" Foreground="Black" Width="25" Height="25"/>
                </Button>
            </Grid>

            <!-- MessageBox Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30*"/>
                    <ColumnDefinition Width="70*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Name="iconMsg" Grid.Column="0" Kind="Error" Foreground="DarkBlue" Width="50" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <TextBlock Name="txtMessage" Grid.Column="1" Text="Message Box Content" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontSize="14" />
            </Grid>

            <!-- MessageBox Footer -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="1" Background="Transparent" BorderThickness="0">
                    <Border.OpacityMask>
                        <VisualBrush>
                            <VisualBrush.Visual>
                                <Border Height="35" Width="100" Background="White"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Border.OpacityMask>

                    <StackPanel Name="ButtonsPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
            </Grid>
        </Grid>
    </Border>

</Window>
